// Retain only the historical key for compatibility with user-provided data.
// The application does not provide a source URL or create a site for this key.
public enum BuiltInVodSourceCatalog {
    public static let speedDirectKey = "test_built_in_cms"
}
